# Starter experiment

No external teaching files are used in this experiment. The notebook generates
the classroom sentences, reserves fixed evaluation prefixes, and then splits the
remaining unique passages. This README is ignored by the corpus loader.
