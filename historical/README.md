# Earlier assignment version — not current evaluation evidence

`previous-baseline/` preserves the completed experiment from the earlier
assignment version. Its generated corpus was not filtered using the new fixed
evaluation prefixes. Do not use these weights or losses as the current starter
benchmark, or attach new evaluation scores to this old run.

The revised submission uses fresh, separated experiments in `results/starter/`
and `results/expanded/`. Only those two experiments contribute to the four-stage
comparison in the root README. Historical files never enter the training folder.
